// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model ChatSession {
  id        Int      @id @default(autoincrement())
  sessionId String   @unique @db.VarChar(255)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  messages  ChatMessage[]

  @@index([sessionId])
}

model ChatMessage {
  id         Int          @id @default(autoincrement())
  sessionId  String       @db.VarChar(255)
  sessionRef Int
  role       String       @db.VarChar(50) // "user" or "assistant"
  content    String       @db.Text
  createdAt  DateTime     @default(now())
  session    ChatSession  @relation(fields: [sessionRef], references: [id], onDelete: Cascade)

  @@index([sessionId])
  @@index([sessionRef])
}

model KnowledgeBase {
  id        Int      @id @default(autoincrement())
  category  String   @db.VarChar(100) // "About", "Services", "FAQ", "Contact", etc.
  question  String?  @db.Text
  answer    String   @db.Text
  source    String   @db.VarChar(50) // "excel" or "website"
  metadata  Json?    // Additional metadata as JSON
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([category])
  @@index([question(length: 100)])
  @@index([category, question(length: 100)])
}

model Analytics {
  id          Int      @id @default(autoincrement())
  question    String   @db.VarChar(500)
  count       Int      @default(1)
  lastAsked   DateTime @default(now())
  createdAt   DateTime @default(now())

  @@unique([question])
  @@index([count])
}

